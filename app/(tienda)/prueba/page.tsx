import { FormField, FormItem, FormControl, FormLabel } from "@/components/shadcn";
import { Input, Label, RadioGroup, RadioGroupItem, Checkbox, Select, SelectTrigger, SelectValue, SelectContent, SelectItem , Button} from "@/components/shadcn";
import { CreditCard, EuroIcon, Apple } from "lucide-react";

export default function NamePage() {

  return (
  <>
  
  </>
  );
}